# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "build/**"
    - "ios/Pods/**"
    - "android/.gradle/**"
  errors:
    invalid_annotation_target: ignore
    # Suppress deprecated_member_use for share_plus package
    # The package marks Share as deprecated but doesn't provide working SharePlus API
    deprecated_member_use: ignore
    # IDE false positives - GameService is properly defined and imported
    undefined_class: ignore
    undefined_method: ignore
    undefined_function: ignore
    non_type_as_type_argument: ignore
    unchecked_use_of_nullable_value: ignore
  strong-mode:
    implicit-casts: false
    implicit-dynamic: false
  # Note: strict language checks are enabled but may generate errors in existing code
  # These should be addressed gradually through refactoring
  language:
    strict-casts: false  # Disabled to avoid breaking existing code - enable gradually
    strict-inference: false  # Disabled to avoid breaking existing code - enable gradually
    strict-raw-types: false  # Disabled to avoid breaking existing code - enable gradually

linter:
  # The lint rules applied to this project can be customized in the
  # section below to disable rules from the `package:flutter_lints/flutter.yaml`
  # included above or to enable additional rules. A list of all available lints
  # and their documentation is published at https://dart.dev/lints.
  #
  # Instead of disabling a lint rule for the entire project in the
  # section below, it can also be suppressed for a single line of code
  # or a specific dart file by using the `// ignore: name_of_lint` and
  # `// ignore_for_file: name_of_lint` syntax on the line or in the file
  # producing the lint.
  rules:
    # Style preferences
    prefer_single_quotes: true
    prefer_const_constructors: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: false  # Disabled to reduce noise
    prefer_final_locals: true
    prefer_final_in_for_each: true
    require_trailing_commas: true
    prefer_collection_literals: true
    prefer_conditional_assignment: true
    prefer_if_null_operators: true
    prefer_inlined_adds: true
    prefer_spread_collections: true
    
    # Performance optimizations
    avoid_unnecessary_containers: true
    prefer_const_constructors_in_immutables: true
    prefer_is_empty: true
    prefer_is_not_empty: true
    avoid_function_literals_in_foreach_calls: true
    prefer_for_elements_to_map_fromIterable: true
    prefer_iterable_whereType: true
    
    # Best practices
    avoid_print: false  # Keep enabled for debug - use kDebugMode checks
    always_use_package_imports: true
    avoid_relative_lib_imports: true
    avoid_web_libraries_in_flutter: true
    avoid_empty_else: true
    avoid_init_to_null: true
    avoid_null_checks_in_equality_operators: true
    avoid_renaming_method_parameters: true
    avoid_return_types_on_setters: true
    avoid_returning_null_for_void: true
    avoid_shadowing_type_parameters: true
    avoid_slow_async_io: true
    avoid_type_to_string: true
    avoid_types_as_parameter_names: true
    # Resource disposal - critical for memory management
    # cancel_subscriptions ensures StreamSubscriptions are cancelled
    # Note: TextEditingController, FocusNode, Timer disposal should be verified
    # manually or via scripts/verify_resource_disposal.dart
    cancel_subscriptions: true
    close_sinks: true
    control_flow_in_finally: true
    empty_catches: true
    empty_constructor_bodies: true
    hash_and_equals: true
    no_adjacent_strings_in_list: true
    no_duplicate_case_values: true
    prefer_asserts_in_initializer_lists: true
    prefer_generic_function_type_aliases: true
    prefer_typing_uninitialized_variables: true
    provide_deprecation_message: true
    sort_constructors_first: false  # Disabled to reduce noise
    sort_unnamed_constructors_first: false  # Disabled to reduce noise
    unawaited_futures: true  # Enabled to catch risky async patterns (e.g., LanguageService constructor)
    use_build_context_synchronously: true
    use_key_in_widget_constructors: true
    use_late_for_private_fields_and_variables: true
    use_named_constants: true
    use_rethrow_when_possible: true
    use_string_buffers: true
    use_test_throws_matchers: true
    valid_regexps: true
    
    # Security and reliability
    avoid_types_on_closure_parameters: true
    prefer_mixin: true
    unnecessary_await_in_return: true
    unnecessary_this: true
    
    # Documentation (optional - can enable if needed)
    # public_member_api_docs: false

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
